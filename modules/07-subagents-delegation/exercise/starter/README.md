# Module 07 Starter Code

## Sample code
- `sample-code/` contains a couple small JS files to review.

## Templates
- `subagent-templates/` contains Markdown + YAML-frontmatter templates for `.claude/agents/`.

## Optional
- `pipeline-starter.ts` is a sketch if you want to automate fan-out/aggregation.
