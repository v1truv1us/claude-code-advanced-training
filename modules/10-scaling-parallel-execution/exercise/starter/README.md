# Module 10 Starter Code

## Files
- `legacy-code/` — 60 legacy JS files (batch target)
- `batch-starter.ts` — starting point for the modernizer
- `templates/` — CI templates (e.g. GitHub Actions)

## Goal
Practice discovery + batching + parallel fan-out and safe budget caps when running non-interactively (`claude -p`).
