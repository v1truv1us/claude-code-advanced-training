# Module 09 Starter Artifacts

This module is about configuring and using MCP servers.

Starter folders:
- `mcp-config/` — example `.mcp.json` and config notes
- `workflow/` — workflow skeleton / scripts
- `database/` — sample DB artifacts

Reference: https://code.claude.com/docs/en/mcp
