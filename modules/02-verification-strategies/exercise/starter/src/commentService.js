// Starter stub. You will implement these using TDD.

export function createComment(_input, _state = {}) {
  throw new Error('NotImplemented');
}

export function replyToComment(_parentId, _input, _state = {}) {
  throw new Error('NotImplemented');
}
